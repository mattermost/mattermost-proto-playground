# @mattermost/compass-proto

**Unpublished** prototyping UI for Compass — mobile shells, full-screen layouts (`ChannelShell`), and call composites.

Not part of the published `@mattermost/compass-ui` core. Peer-depends on `compass-ui` + `compass-icons`. Intended for the design-system monorepo / playground; do not publish to npm.
