The Call popout layout preview shows only the windowed call surface. The
wrapper is layout-only (no card chrome) so the doc shell shows through around
the popout.

Use the Specimen tab for the live layout.

> Documentation in progress.
