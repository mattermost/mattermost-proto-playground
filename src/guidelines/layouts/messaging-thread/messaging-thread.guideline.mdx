The Channel (thread) layout is the same full channel shell as the Channel
layout, with the right-hand thread column open: `RightSidebarHeader` titled
Thread, the thread post stream, and a reply composer in the sidebar footer.

> Documentation in progress.
