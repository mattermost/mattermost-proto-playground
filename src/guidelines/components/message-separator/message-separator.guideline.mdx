import { Message } from '@mattermost/compass-ui';
import { messageStyles } from '@mattermost/compass-ui';
import { MessageSeparator } from '@mattermost/compass-ui';
import avatarLeonard from '@/assets/avatars/Leonard Riley.png';
import avatarAikoTan from '@/assets/avatars/Aiko Tan.png';

Message Separators sit between messages in the stream to group them — by
day, by reply count, or to mark the boundary between read and unread. They
help readers orient themselves when scrolling back through history or
returning to a busy channel.

<Divider />

## Anatomy

A horizontal line broken by a centered label. The label sits inline with the
line; nothing else competes with it.

- <Num>1</Num> **Line** — full-width hairline, behind the label.
- <Num>2</Num> **Label** — short text on top of the line. Date, "New
  Messages", or a reply count.
- <Num>3</Num> **AI Summarize** — optional button on the New Messages
  variant that triggers an AI summary of unread activity.

<AnatomyStage style={{flexDirection:'column',gap:'1rem',alignItems:'stretch'}}>
  <MessageSeparator type="Date" label="Today" />
  <MessageSeparator type="New Messages" />
  <MessageSeparator type="Reply Count" label="4 replies" />
</AnatomyStage>

<Divider />

## Variants

Three variants, three jobs. Don't combine them — pick the one the moment
calls for.

| Variant      | Use when                                                                 |
| ------------ | ------------------------------------------------------------------------ |
| Date         | Dividing messages by day in the stream.                                  |
| New Messages | Marking the boundary between messages the user has read and new ones.    |
| Reply Count  | Inside a thread, showing how many replies follow.                        |

### Date labels

Date labels follow the user's locale and shorten when they can:

- **Today** and **Yesterday** for the most recent two days.
- **Friday**, **Monday**, etc. for the last week.
- **February 11** for older dates in the current year.
- **February 11, 2018** for prior years.

<AnatomyStage style={{ margin: 'var(--spacing-m) 0' }}>
  <MessageSeparator type="Date" label="Today" />
</AnatomyStage>

### New Messages

The New Messages separator is a single, durable landmark that stays where the
user left off until they scroll past it. Use the `Summarize` action only when
AI summaries are enabled.

<div style={{padding:'1rem',background:'var(--center-channel-bg)',border:'1px solid rgba(var(--center-channel-color-rgb),0.08)',borderRadius:'8px'}}>
  <MessageSeparator type="New Messages" showAiSummary />
</div>

### Reply Count

In a thread (including the right-hand reply view), place this separator under
the first message so readers see how many replies follow. The label aligns
with the message text column: a short line spans the avatar column, then the
count, then the line runs to the edge—unlike the date separator, the copy is
not centered across the full width.

<AnatomyStage
  style={{
    margin: 'var(--spacing-m) 0',
    maxWidth: '400px',
    flexDirection: 'column',
    alignItems: 'stretch',
    justifyContent: 'flex-start',
  }}
>
  <div
    style={{
      width: '100%',
      boxSizing: 'border-box',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'stretch',
      background: 'var(--center-channel-bg)',
      border: '1px solid rgba(var(--center-channel-color-rgb), 0.08)',
      borderRadius: '8px',
    }}
  >
    <Message
      avatarSrc={avatarLeonard}
      avatarAlt="Leonard Riley"
      username="Leonard Riley"
      timestamp="Today at 9:41 AM"
      showMessageActions={false}
    >
      <p className={messageStyles['message__body-text']}>
        Quick gut-check on the thread header—should the parent channel always show
        as a secondary title?
      </p>
    </Message>
    <MessageSeparator type="Reply Count" label="7 replies" />
    <Message
      avatarSrc={avatarAikoTan}
      avatarAlt="Aiko Tan"
      username="Aiko Tan"
      timestamp="Today at 9:48 AM"
      showMessageActions={false}
    >
      <p className={messageStyles['message__body-text']}>
        I&apos;d show it whenever there&apos;s a meaningful parent—threads, pins,
        files—and skip it for global views like Saved Messages.
      </p>
    </Message>
  </div>
</AnatomyStage>

<Divider />

## Implementation

For props, variants, and copy-ready examples, see the
[Message Separator specimen page](/components/message-separator/specimen).
