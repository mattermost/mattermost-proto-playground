The Profile Popover is an overlay surface that summarises a user's
profile and exposes quick actions — message, mention, view profile —
without taking the user out of the current screen.

> Documentation in progress.
